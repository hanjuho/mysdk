#pragma once



#include "../../hsdk_common.h"



namespace hsdk
{
	namespace game
	{
		namespace i
		{

			DECL_STRUCT(i_DataTable);
			DECL_STRUCT(i_Controller);
			DECL_STRUCT(i_ActionLayer);
			DECL_STRUCT(i_ActionListener);
			DECL_STRUCT(i_ModelRenderer);
			DECL_STRUCT(i_GameObject);
			DECL_STRUCT(i_GameEngine);

		}
	}
}